{% block stylesheets %}
    <link rel="stylesheet" href="css/navbarUser.css">
{% endblock %}


<header id="top">
    <div class="container-fluid">
        <nav class="navbar navbar-expand-md bg-dark navbar-dark fixed-top" id="main-nav">
            <div class="container">
                <a href="{{path ("index")}}" class="navbar-brand"> <img src="{{asset ("img/logo-monvoisin-blanc.png")}}" alt="logo" width="250px"> </a>
                <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item mr-1">
                            <a href="{{ path ('serviceGet') }}" class="nav-link">Voir les services en cours</a>
                        </li>
                        <li class="nav-item mr-1 pt-2">
                            <div class="dropdown">
                                <a class="dropdown-toggle text-custom" href="#"  id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                  Mon profil
                                </a>
                              
                                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                  <a class="dropdown-item" href="{{ path ('userProfile', {id: user.id}) }}">Mon profil</a>
                                  <a class="dropdown-item" href="{{ path ('dashboardUserService', {id: user.id})}}">Accéder à mes services</a>
                                  <a class="dropdown-item" href="{{ path ('mailbox', {id: user.id})}}">Accéder à ma messagerie</a>
                                  <a class="dropdown-item" href="{{path ("faqs")}}">Faqs</a>
                                  <a class="dropdown-item" href="{{path ("contactAdd")}}">Contact</a>
                                  <a class="dropdown-item" href="{{path ('app_logout')}}">Se déconnecter</a>
                                </div>
                              </div>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </div>
</header>